local players = game:GetService("Players")

for _, player in pairs(players:GetPlayers()) do
    game.Players:ReportAbuse(player, "Exploiting", "User was recently IP banned and terminated from roblox for selling robux offsite, he now has bypassed the IP ban with proxy and made a account to break the tos")
end
